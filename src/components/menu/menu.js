import React, { Component } from 'react';
import AppBar from '../appBar/AppBar';

class Menu extends Component {
  render() {
    return (
      <div className="Menu">
        <AppBar />
      </div>
    );
  }
}

export default Menu;
